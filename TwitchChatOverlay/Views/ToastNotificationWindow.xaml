<Window x:Class="TwitchChatOverlay.Views.ToastNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ToastNotification" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="380"
        SizeToContent="Height"
        Opacity="0">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.2"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="8"
            Margin="5"
            Background="#CC1A1A2E"
            Effect="{x:Null}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- カラーバー -->
            <Border Grid.Column="0"
                    Background="{Binding ThemeColorBrush}"
                    CornerRadius="8,0,0,8"/>

            <!-- コンテンツ -->
            <StackPanel Grid.Column="1" Margin="10,10,10,10">
                <!-- タイプアイコン + ユーザー名 -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding TypeIcon}"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding Username}"
                               FontWeight="Bold"
                               FontSize="13"
                               Foreground="{Binding UserColorBrush}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="260"/>
                </StackPanel>

                <!-- メインテキスト -->
                <TextBlock Text="{Binding DisplayText}"
                           Foreground="#FFFFFF"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"
                           MaxHeight="60"/>

                <!-- サブテキスト（オプション） -->
                <TextBlock Text="{Binding SubText}"
                           Foreground="#AAAAAA"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Margin="0,3,0,0"
                           Visibility="{Binding HasSubText, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
